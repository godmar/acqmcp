apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: alma-mcp
  labels:
    app.kubernetes.io/name: alma-mcp
spec:
  rules:
    - host: example.invalid
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: alma-mcp
                port:
                  number: 80

apiVersion: v1
kind: Service
metadata:
  name: acq-mcp
  labels:
    app.kubernetes.io/name: acq-mcp
spec:
  selector:
    app.kubernetes.io/name: acq-mcp
  ports:
    - name: http
      port: 80
      targetPort: http

apiVersion: v1
kind: Service
metadata:
  name: alma-mcp
  labels:
    app.kubernetes.io/name: alma-mcp
spec:
  selector:
    app.kubernetes.io/name: alma-mcp
  ports:
    - name: http
      port: 80
      targetPort: http
